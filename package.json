{
  "name": "github-pages-minify-test",
  "version": "1.0.0",
  "description": "A test repository for GitHub Pages with GitHub Actions for minify.",
  "main": "index.js",
  "scripts": {
    "minify": "mkdir -p dist && html-minifier-terser --config-file html-minifier.json -o dist/index.html src/index.html && clean-css-cli -o dist/style.css src/style.css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "html-minifier-terser": "^7.1.0"
  }
}
